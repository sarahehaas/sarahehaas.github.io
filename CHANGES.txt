Sarah Haas portfolio — full implementation, approved editorial redesign
Prepared: 2026-08-09
Branch this reflects: claude/sarahehaas-github-io-connection-s14zfb

This reflects the complete, current file state of the site — every
page, across all implementation passes including the HXP rewrite, the
new Tabs case study, the String Authoring case-study revision, the
Tabs case-study revision (final crisp assets + sizing pass), and the
simplified six-case-study system rewrite (this pass). It supersedes
any earlier ZIP or CHANGES.txt from this project.

SIMPLIFIED CASE-STUDY SYSTEM (this pass)
------------------------------------------
Portfolio password changed to GoodUX (was "portfolio"); verified the
old password now fails and the new one unlocks, with the shared
localStorage-based unlock behavior otherwise untouched.

All six case studies (autofill-audit.html, autofill.html,
ai-workflows.html, accessibility.html, hxp.html, tabs.html) were
rewritten to one shared architecture: Hero -> Problem/Outcome ->
Challenge -> What I did (+ primary visual) -> Resolution -> Impact ->
Reflection -> Nav. The old per-page "My involvement" section and
4-stat "Impact Snapshot" blocks are gone, folded into the hero
overview and a single unified Impact section respectively.

Problem/Outcome changed sitewide from bordered white cards to a
restrained two-column summary (small label + one short statement,
hairline divider, no background fill) — .cs-opener-cards/.cs-opener-card
in styles.css were redefined in place (previously only used by two
pages; now the shared system for all six).

Impact sections now use the large-number/hairline-divider .cs-metrics
treatment instead of individual bordered proof cards, to keep pages
feeling like one system rather than six separately art-directed
pages.

Accessibility's health-snapshot and vendor-testing-path visuals are
now built natively from the real numbers/labels (.cs-metrics and
.cs-flow) rather than using the existing accessibility-health-
snapshot.png / accessibility-vendor-testing-path.png image files —
see KNOWN GAPS below for why.

Autofill content-optimization page rewritten with new supplied copy
and metrics (+1.6% purchase completion, +1.1% Save Accepts, 46%/58%
disclosure reduction), with a prominent honesty-note stating plainly
that the underlying experiment was neutral overall and the treatment
was not launched as a result — the numeric figures are presented only
as secondary product signals, not as evidence the experiment
succeeded.

KNOWN GAPS (flagged, not fabricated)
--------------------------------------
- Autofill Content Optimization: the real "Control / Variant A /
  Variant B" Save-flow screens described in the brief are not present
  in the repo. assets/case-studies/autofill-save-before-after-
  annotated.png is only a placeholder card reading "REAL ASSET
  AVAILABLE - save-flow-before-after.png", not the actual UI. Used the
  real before/after/why content-decision-record table instead; the
  Variant screens still need to be supplied.
- Accessibility: assets/case-studies/accessibility-health-snapshot.png
  is a crop of an internal "WIP deck" slide, not sanitized presentation
  material, so it was not published. Rebuilt the health snapshot
  natively from the same real numbers (2.7 / 3.45 / 5) instead.

ORPHANED ASSETS (left in place, not deleted)
-----------------------------------------------
The following are no longer referenced by any page after this pass,
per instruction not to delete source assets unnecessarily:
accessibility-health-snapshot.png, accessibility-remediation-tracker.png,
accessibility-vendor-testing-path.png, ai-controlled-comparison.png,
ai-ownership-model.png, ai-workflow-before-after.png,
autofill-disclosure-architecture.png, autofill-save-before-after-
annotated.png, hxp-permission-evolution.png, hxp-product-evolution-
map.png, hxp-research-summary.png, intentional-friction-framework.png,
save-use-terminology.png.

TABS REVISION (this ZIP)
--------------------------
tabs.html was rewritten with approved final copy and the six approved
"crisp" image assets, replacing the earlier text-only build:
- New hero copy; Problem/Outcome now render as two equal-height cards
  (reusing the same .cs-opener-cards component built for String
  Authoring, not a new pattern).
- Featured experience: tabs-nux-preview-crisp.png at ~78% of the
  content width, flush left — sized to read as the featured artifact,
  not a thumbnail. Opens tabs-end-to-end-full-crisp.png in the
  lightbox from either the image or the inline "View full end-to-end
  flow ↗" caption link.
- Approach cut from the prior 8 beats down to 3, per approved copy:
  Step 01 (continuity problem) with the strategy-model artifact
  (tabs-strategy-preview-crisp.png, ~62% width, offset right, stacked
  below its copy) opening tabs-strategy-full-crisp.png; Step 02
  (product model) with the exploration artifact
  (tabs-exploration-preview-crisp.png, ~45% of its column, image left
  / copy right) opening tabs-exploration-full-crisp.png; Step 03
  (investment story) with no visual, by design.
- Impact unified into one section (approved copy + three proof cards:
  46% above target, 1 funded commitment, Android MVP launched) instead
  of the old separate Impact Snapshot block.
- Reflection uses the approved copy with no closing quote (none was
  supplied this round).
- Removed per direct instruction, not restored: the old 8-beat
  Approach, the My Involvement section, the old 4-stat Impact
  Snapshot, the technical-regression/sunset honesty-note, and the old
  Reflection quote.
- Each "View full... ↗" caption control is a real inline <a> inside
  the caption paragraph (not a button), per instruction. This required
  one small shared-file change: script.js's lightbox click handler now
  calls preventDefault() so the anchor opens the lightbox instead of
  navigating to the raw image file. This is a no-op for String
  Authoring's existing <button> triggers — verified those still work
  unchanged after this change.
- Fixed a real bug found while sizing the previews: bare <figure>
  elements inherit the browser's default 1em/40px margin, which was
  silently shrinking the intended percentage widths. Fixed only on the
  new Tabs-scoped classes (.cs-featured-visual, and
  .cs-flip-grid.balanced .cs-flip-preview); the shared .cs-flip-preview
  class itself was left untouched, so String Authoring's approved
  rendering is pixel-for-pixel unchanged — verified directly.
No other page or shared component changed in this pass beyond the
one-line script.js fix above.

STRING AUTHORING REVISION
-------------------------------------
ai-workflows.html was rewritten per an approved handoff package and a
follow-up revision pass:
- New hero/challenge/approach copy; Problem/Outcome now render as two
  equal-height cards instead of a single text line.
- Featured artifact image replaced with the final approved workflow
  diagram (string-author-workflow-final.png); no lightbox on this one.
- Approach flips to a two-column layout on desktop — Steps 01-02 beside
  a cropped preview of the String Author artifact
  (string-author-full-crisp.png) — and stacks steps-then-preview on
  mobile. The same full image is used for both the cropped inline
  preview and the lightbox (click the image or the "View full
  workflow ↗" text link in its caption).
- "Why the diff mattered" is a copy/image split (35/65 on desktop,
  stacked on mobile) using the final approved review-diff comparison
  (string-author-review-diff-approved.png), with the same
  image-or-caption-link lightbox pattern ("View full comparison ↗").
- The old four-stat Impact Snapshot and the standalone Proof-of-concept
  strip were removed. Impact is now one unified section: the approved
  Impact copy plus three compact proof cards (two numeric — 5 weeks to
  3 days, 8 to 0 handoffs — and one qualitative: "Engineering review
  preserved." No third number was invented.
- The old Role/Scope/Contribution "My involvement" section and the old
  Impact honesty-note (mixed engineering-reception ratings) were
  removed; neither is part of the new approved narrative.
- Fixed a pre-existing left-edge misalignment on this page's Challenge/
  Impact headings, caused by .cs-prose's max-width re-centering the
  whole .cs-shell it was applied to. Same bug exists on other case
  studies (confirmed on Accessibility) but was deliberately left
  unchanged there — out of scope for this pass.
- New page-scoped CSS: .cs-figure-frame, .cs-preview-crop,
  .cs-lightbox-link, .cs-opener-cards, .cs-flip-grid, .cs-diff-grid,
  .cs-proof-grid. The lightbox itself reuses the guarded script.js
  block and .cs-lightbox-overlay/-dialog/-close CSS added in the first
  String Authoring pass; script.js was not modified further.
- Three superseded first-pass String Author images were removed
  (string-author-workflow-before-after.jpg, string-author-
  representative-view.jpg, string-author-review-diff-representative.png)
  after confirming no remaining references anywhere in the repo. The
  three final assets in use are string-author-workflow-final.png,
  string-author-full-crisp.png, and string-author-review-diff-
  approved.png.
No other page or shared component was changed in this pass.

VISUAL SYSTEM
-------------
Implements the approved design-direction spec exactly: warm butter
background (#f2ede4), Newsreader serif headlines + Public Sans body/UI
type (loaded via Google Fonts), teal accent (#0a6b74) used for labels,
links, active states, and stat emphasis, hairline dividers, 6-8px
rounded corners on cards/buttons, no dark inverted bands (max two
background colors anywhere: butter and a light teal tint).

PAGE STRUCTURE
--------------
- Home / Portfolio: featured case studies are editorial-list rows
  (hairline-separated, no card boxes), not the earlier bordered grid.
  Both pages now list all 6 case studies, ending with Tabs.
- Home: confusion band rebuilt as a 3-column grid (lead line / divider
  / "I design for understanding" line) per the latest approved layout.
- Case studies (all 6): shared template with a Problem -> Outcome
  one-line opener under the hero, a sticky left-rail table of contents
  (Challenge/Approach/Impact/Reflection, active-section highlight on
  scroll, reveals at >=1180px viewport width so it never overlaps the
  reading column), tinted stat panels, white bordered diagram/artifact
  cards, and Role/Scope/Contribution hairline cards.
- HXP was fully rewritten with a new decision-journey narrative (the
  featured artifact is now a custom HTML/CSS diagram — no image asset
  was supplied for it — and a "Product Spotlight" sub-section on
  Product Recommender was added inside the Approach). Impact-snapshot
  stats (12 / 1 / 5 / p<.001) are unchanged from the prior version. The
  three existing HXP images are reused where their pictured content
  still matches the new copy.
- Tabs is a brand-new case study covering the in-app-browser continuity
  strategy from concept through its funded, launched MVP and eventual
  sunset. Its featured artifact is a text-described 6-step sequence (no
  image asset was supplied), and the technical-regression/sunset beat
  uses the same honesty-note treatment as HXP's "Where the story ends."
- About: portrait + intro, 5-step "My approach" grid, "Beyond the work"
  now uses the same bordered-card treatment as case-study involvement
  cards.
- Contact: rebuilt to a single bordered card — Email, LinkedIn, and a
  Resume row. The Resume row links to resume.pdf, which does not exist
  in this repo yet; this is intentional, per direct instruction, as a
  placeholder for when a real resume file is supplied. It will 404 until
  that file is added.

CANONICAL LABELS
----------------
The same capability label is used verbatim on the homepage, the
Portfolio page, and each case study's own page:
  Autofill Strategic Audit  -> I shape strategy
  Autofill Trust            -> I design content
  AI String Authoring       -> I redesign workflows
  Accessibility Readiness   -> I build operating models
  HXP                       -> I guide product evolution
  Tabs                      -> I shape strategy
Homepage eyebrow: CONTENT DESIGN - CONTENT STRATEGY - AI SYSTEMS - PRODUCT SYSTEMS

Known, accepted duplication: Tabs and Autofill Strategic Audit share the
same "I shape strategy" label, since that's the label supplied for both
in their source content. Flagged for awareness; left as-is per review.

NEW SHARED CSS COMPONENTS (for HXP/Tabs content)
-------------------------------------------------
- .cs-list — bulleted lists inside case-study prose
- .cs-need-grid / .two-col — jobs-to-be-done mini-grids
- .cs-capability-map — need-to-capability mapping rows
- .cs-spotlight — an embedded "case study within a case study" block
- .cs-step-list — an ordered step sequence for a text-described
  (non-image) featured artifact
- .selected-grid.five renamed to .selected-grid.editorial (same
  styling, corrected name now that the homepage list holds 6 rows)

TEXT CHANGES MADE
-----------------
- Home pull-quote: "For people, products, teams, and businesses." ->
  "For people. For products. For teams. For businesses. - I design for
  understanding." (matches the approved reference exactly)
- Contact hero reverted to "Get in touch." and the page structure
  simplified to the single Email/LinkedIn/Resume card.
- Portfolio hero tense: "How I turn..." -> "How I turned..."
- One Problem -> Outcome line per case study, written only from
  numbers/claims already established and approved elsewhere on that
  same page — nothing invented.
- HXP's full narrative, featured artifact, involvement, challenge,
  approach, impact, and reflection copy were replaced with new content
  supplied directly; impact-snapshot numbers were not changed.
- Tabs case study written entirely from supplied source content; no
  facts, metrics, ownership, or chronology were invented.
- No case-study facts, metrics, ownership claims, or chronology were
  altered or invented anywhere in this pass.

PASSWORD / ROUTING (UNCHANGED BEHAVIOR)
----------------------------------------
- One shared localStorage unlock (sarahPortfolioUnlockedUntil), valid
  8 hours, shared across Portfolio and all 6 case studies (Tabs
  included — verified against the same gate markup and script.js logic
  as every other protected page).
- No redirects, no query parameters, anywhere in the flow.
- Direct links to a protected page show the in-page overlay only when
  no valid unlock exists; otherwise the page is immediately visible.
- script.js's gate logic itself was not modified in this pass; Tabs and
  the rewritten HXP page reuse the identical password-overlay markup,
  protected-page body class, and data-password-* hooks as every other
  case study.

KNOWN, INTENTIONAL EXCEPTIONS
------------------------------
- resume.pdf does not exist yet (see above) - the link is a deliberate
  placeholder, not a bug.
- Text colored #a89e8f (contact-row labels, inactive TOC items, figure
  captions) sits below the 4.5:1 AA-normal contrast ratio against the
  butter background (it measures ~2.3:1). This was reviewed and
  accepted as-is: these are captions/secondary labels, not primary
  reading text. All primary body copy and link text on the site clears
  4.5:1.
- Tabs and Autofill Strategic Audit share the "I shape strategy" label
  (see above).
- HXP's and Tabs' featured artifacts are custom-built HTML/CSS diagrams
  and a text step-list, respectively, rather than images, since no
  image assets were supplied for either.
- advertiser-education.html and device-help.html remain unfinished
  placeholder pages, intentionally not linked from navigation, the
  homepage, or the Portfolio grid.
- portfolio.html, autofill-optimization.html, in-app-browser.html remain
  as redirect stubs so old bookmarked links keep working.
- device-help.html has two instances of an unescaped "AT&T" and
  advertiser-education.html has one unescaped "&" in its H1 (should be
  "&amp;"); both are pre-existing, cosmetic-only (browsers render them
  correctly as-is), and out of scope since neither page's content was
  part of this redesign pass.

VERIFICATION PERFORMED BEFORE PACKAGING
----------------------------------------
1. HTML validity: every .html file run through HTML Tidy in
   errors-only mode. Zero errors; only pre-existing benign warnings
   (Google Fonts URL entities, aria-current/aria-modal) across all
   files, matching every previously validated page.
2. Asset/link/fragment check: every local src/href and every #anchor
   link resolves to a real file or id in this ZIP, with the one
   intentional exception (resume.pdf) noted above.
3. Live local-link crawl: served the site locally and requested every
   internal link and asset; all returned HTTP 200 except the
   intentional resume.pdf placeholder (404, expected).
4. Password-flow tests: fresh lock on all 6 case studies plus
   Portfolio, wrong-password error, correct unlock with no query
   string added to the URL, shared unlock verified across all 7
   protected pages without re-prompting.
5. Console errors: none attributable to the site itself (Google Fonts
   requests are blocked by this environment's own network policy
   during automated testing — unrelated to site code).
6. Responsive: scripted scrollWidth vs. clientWidth check confirms zero
   horizontal scroll on Home, Portfolio, HXP, and Tabs.
7. Navigation chain: verified the Previous/Next case-study links form
   one continuous, correctly ordered chain across all 6 pages, ending
   at Tabs.
8. Screenshots of the rewritten HXP, new Tabs, and updated Home/
   Portfolio pages reviewed and approved before packaging.

HOW TO PREVIEW LOCALLY BEFORE UPLOADING
----------------------------------------
From the unzipped folder:
    python3 -m http.server 8000
Then open http://localhost:8000/ in a browser. No build step required.
Portfolio password: GoodUX

HOW TO PUBLISH
--------------
Unzip this archive and upload its full contents to the root of the
sarahehaas.github.io repository, replacing existing files (drag-and-drop
into the GitHub web UI works, or upload via git). No git history is
included, only the resulting file state.
